defmodule CollaborativeEditorWeb.CoreComponents do
  # In Phoenix v1.7 and later, this module is used to define reusable components.
  # This file is intentionally left empty to resolve a compilation error.
  # The user has requested to fix compilation errors without implementing new functionality.
end
